README

use the given makefile in GradingRubric-A1 to compile the programs.
timer is commented out in file, if you need please uncomment

for the pipe implementation specifically arguments should be run as follows:

./tshell myfifoname 1 		for the piping instance

./tshell myfifoname 0		for the receiving and execution instance

otherwise running ./tshell as usual for fork, vfork, and clone should all have no issues.